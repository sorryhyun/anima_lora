"""scenes — the ``scenes`` stage (plan_synth S line).

stage        prompt vocabulary, frames, generation → scenes_<tag>/img
judge        detector + readers keep / reject, scenes.jsonl, sheets, report
"""
