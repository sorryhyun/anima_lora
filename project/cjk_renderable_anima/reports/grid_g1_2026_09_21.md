# grid G1 — the 374-row gate arms for the run that replaces step 1 (2026-09-21)

> `step1_0920`'s inventory (374 rows, 452 pool units, `*1` weights), raw pack,
> cold, σ 0.7–0.9, cosine, batch 4, `--box_share 0.25`, scene singles + single-
> cell grid items, the deck dealt by pool weight. **Even grids, 50 %, plain is
> the recipe: 6 000 steps read `step1_0920`'s kana identity (20 / 12 vs 20 / 8)
> at a ninth of its steps. A denser grid mix loses identity (37 → 28 over four
> groups); ΔFM on the scene batches keeps the scene but repeats the glyph
> (native 17 / 11 vs 20 / 25); 75 % grid at matched total exposure and 27 %
> fewer steps collapses transfer (`single` 12, native 5 / 4) while its in-frame
> read is the best of the four — scene draws are what carry a row out of the
> grid frame, and they cannot be traded for cell draws.** Kanji are unmoved in
> every arm (2–4/36). Plain's native hits are inflated by scene wipes (sheets).

| arm | grids | share | loss, lr | steps | scene / cell draws per row | `single` / ext / small / kanji (of 36) | `single` seed 0 / 1 | native `en` / `swap` (both, of 64) | en cos `en` · `swap` | in-frame found k = 4 (flat · bubble) | wall |
|---|---|---|---|---|---|---|---|---|---|---|---|
| `rows_g1_even_p6k` | `2x2,3x3,2x3,3x2` | 50 % | plain 1e-3 | 6 000 | 32 / 202 | **20 / 12 / 3 / 2** | 14 / 6 | **20 / 25** | 0.922 · 0.947 | 0.56 · 0.59 | 66 min |
| `rows_g1_dense_p6k` | `2x2:1,3x3:2,2x3:2,3x2:2` | 50 % | plain 1e-3 | 6 000 | 32 / 211 | 15 / 10 / 1 / 2 | 12 / 3 | 25 / 20 | 0.914 · 0.952 | 0.66 · 0.59 | 66 min |
| `rows_g1_even_pair_d6k` | even | 50 % | scene ΔFM + grid plain, 2e-3 | 6 000 | 32 / 202 | 18 / 10 / 0 / 4 | 13 / 5 | 17 / 11 | 0.924 · **0.961** | 0.50 · 0.44 | 78 min |
| `rows_g1_even75_p4k4` | even | 75 % | plain 1e-3 | 4 400 | 12 / 222 | 12 / 12 / 0 / 3 | 10 / 2 | **5 / 4** | 0.934 · 0.968 | 0.62 · 0.59 | 55 min |
| `step1_0920` (53 k, scene-only) | — | 0 % | ΔFM 2e-3 | 53 000 | 568 / 0 | 20 / 8 / 0 / 8 | — | 19 / 8 | 0.934 | — | ≈ 9 h |

Jobs: train + eval `20260921-071912-2ae353`, `…-aed21a`, `20260921-095423-95b604`,
`20260921-112226-d967a3`; natives `…071912-1593e9`, `…-a26cd2`, `…095423-c792a5`,
`…112227-dfbc64`; in-frame `…071912-cd8ecd`, `…-0cca1b`, `…095423-761564`,
`…112227-8422da`. Paired data = the same recipe rebuilt with `--pair_ref en
--pair_ref_pool 4` (`data_g1_even_pair`; the reference pool draws from the main
rng, so its scene items are a different draw of the same recipe). EN 24/24 on
all four.

Reads:

- **Even over dense.** Identity 37 against 28 over the four groups, native
  45 against 45, dense worse on seed 1 (3/18 against 6/18) and its sheets wipe
  more (en cos 0.914). More cells per item is not more identity per step here.
- **Plain over paired — on the count, with a caveat from the sheets.** The
  paired arm travels 1.5 × further (row norm 1.41 against 0.97; lr 2e-3), is
  18 % slower, and ties identity (18 / 10 / 0 / 4). Native falls 20 / 25 →
  17 / 11 — but its scenes are intact where plain's seed-0 renders are a white
  canvas with one large glyph (p01 / p02 / p06 / p07), and its misses are
  repeats of the *right* glyph (ああああ, あ……あ). Under ΔFM the scene batches
  cancel count against the EN sibling, so the only count signal left is the
  grid's "several glyphs". Plain's hits are inflated by the wipe; paired's are
  deflated by the repeat. Neither is a clean native.
- **Scene draws do not trade for cell draws.** The 75 % arm has the 50 % arm's
  total exposure (234 draws per row) at 27 % fewer steps and reads the best
  in-frame `found` of the four (0.62 · 0.59) — the rows are learned — but
  `single` 20 → 12 (seed 1: 2/18) and native 45 → 9. With 12 scene draws per
  row the table stays in the grid frame. **No step-1 time is saved by raising
  the grid share**; what a run can shorten is bounded by its scene draws.
- **Kanji: 2 / 2 / 4 / 3 of 36.** 200 of the 374 rows, unmoved by grid mix,
  loss or share at this exposure; their reads are 3–4 glyphs long. Whether
  that is exposure (the 80 k run gives them 13 ×) or glyph size in a ≈ 100 px
  cell is not separated.
- か reads 0/32 native in both plain 50 % arms at 22 scene draws per row: the
  per-row scene budget is the floor under everything else.

For the run (`../plan_grid.md` M2): even grids, 50 %, plain, 80 k. Read its
native with `--native_floor 1` (hit & scene kept), not hits alone. The
scene-only tail is the one untested lever that addresses both failure shapes
seen here (the wipe and the repeat).
