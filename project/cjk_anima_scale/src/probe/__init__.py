"""probe — the one helper ``eval.cf_sense`` imports from the source line's
``probe/merge_tables.py`` (``row_text_map``), vendored alone so the ruler's
import line stays byte-faithful. None of the source's ``probe/*.py`` scripts
are here."""
