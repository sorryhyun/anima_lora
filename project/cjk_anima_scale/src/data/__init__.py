"""data — the old ``data`` stage's resolvers and renderers the line's builder
(``cjk_scale/recipes.py`` + ``builder.py``) draws with; the stage itself is gone.

stage        ShapePool, the vocab / eval-string resolvers, _ink_stats
vocabs       vocab specs → the Inventory the resolvers fill
inventory    Qwen pieces → pack rows; word / kanji inventories; phrase lines
synth        load_scenes, scene_caption, the sentence / short letter floors
grid         render_grid, _Deck
"""
