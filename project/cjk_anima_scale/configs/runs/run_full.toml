# run_full — the production chain: the whole JA inventory at 30 / 30 / 30
# (design.md § 5), warm from the merged step-1 table (step1_0921 +
# step1_0921z, 2 274 rows — the probe line's `merge_tables.py` output).

run = "run_full"
seed_table = "output/cjk_anima_scale/rows_step1_0921_merged/trained.pt"
seed = 0

[data]
units = ["kana", "kana_ext*1", "small", "kanji:200*1", "list:、,。,・,ー,～,〜,！,？,「,」,！！,・・・,・・・・*1"]
pieces = "ja_cold_0001_1900.txt"           # the multi-glyph rows (assets/units)
phrase_file = "$MANGA109S/derived/dialogue_2_10.tsv"   # export MANGA109S=~/manga109s (or .env)
n_items = 40000

[budget]                                    # steps per row
stage0709 = 30
stage0507 = 30
stage0305 = 30
stage0309 = 30                              # "to set" (design § 5) — the first run reads it
