# Merged config — generated by train.py --print-config
# Method: lora
# Preset: default
# Git: 8581e3eb859181b1cd50dd56f9b72bf887ebb992

# --- from configs/base.toml ---
attn_mode = "flash"
cache_latents = true
cache_latents_to_disk = true
cache_text_encoder_outputs = true
cache_text_encoder_outputs_to_disk = true
dataloader_pin_memory = true
discrete_flow_shift = 1.0
log_every_n_steps = 2
lora_cache_dir = "post_image_dataset/lora"
lr_scheduler = "constant"
masked_loss = true
mixed_precision = "bf16"
network_module = "networks.lora_anima"
network_train_unet_only = true
optimizer_args = [ "fused=True",]
optimizer_type = "AdamW"
output_dir = "output/ckpt"
persistent_data_loader_workers = true
pretrained_model_name_or_path = "models/diffusion_models/anima-preview3-base.safetensors"
qwen3 = "models/text_encoders/qwen_3_06b_base.safetensors"
resized_image_dir = "post_image_dataset/resized"
save_model_as = "safetensors"
save_precision = "bf16"
skip_cache_check = true
source_image_dir = "image_dataset"
static_token_count = 4096
timestep_sampling = "sigmoid"
torch_compile = true
trim_crossattn_kv = false
use_custom_down_autograd = true
vae = "models/vae/qwen_image_vae.safetensors"
vae_chunk_size = 64
vae_disable_cache = true

# --- from configs/presets.toml[default] ---
blocks_to_swap = 0

# --- from configs/methods/lora.toml ---
alpha_rank_scale = 1.0
balance_loss_warmup_ratio = 0.4
balance_loss_weight = 5e-7
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1
checkpointing_epochs = 4
hydra_router_layers = ".*(mlp\\.layer[12])$"
learning_rate = 2e-5
log_with = "tensorboard"
logging_dir = "output/logs"
max_train_epochs = 12
min_rank = 1
network_alpha = 32.0
network_dim = 32
network_router_lr_scale = 10
num_experts = 12
num_sigma_buckets = 4
output_name = "anima_hydra_naive"
per_bucket_balance_weight = 0.3
save_every_n_epochs = 4
sigma_feature_dim = 16
sigma_hidden_dim = 16
sigma_router_layers = ".*(mlp\\.layer[12])$"
use_hydra = true
use_ortho = false
use_shuffled_caption_variants = true
use_sigma_router = true
use_timestep_mask = true

# --- from CLI ---
method = "lora"
