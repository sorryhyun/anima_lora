# comfyui-anima-trainer

ComfyUI custom node that trains an Anima LoRA from **one image + caption** and
returns a ready-to-use `MODEL`, so a single workflow run covers **curate → train
→ generate**.

Complements `https://github.com/sorryhyun/ComfyUI-Anima_lora-Adapter` (which only loads already-trained
adapters). Training runs inside the local Anima **training daemon** — a separate
detached process — so a CUDA OOM kills only the job, not ComfyUI.

### Install shapes

- **Inside the repo** (`<anima_lora_root>/custom_nodes/comfyui-anima-trainer`):
  `__init__.py` adds `<anima_lora_root>` to `sys.path`, so the node imports the
  live daemon client and reads the daemon's pidfile (it follows a fallback
  port).
- **Standalone** (this folder dropped into a separate `ComfyUI/custom_nodes/`):
  the node falls back to the pure-stdlib daemon client bundled under `_vendor/`
  (regenerated by `scripts/release/sync_vendor.py`). With no repo pidfile to read it
  talks to the default port `127.0.0.1:8765` — set `ANIMA_DAEMON_PORT` if your
  daemon binds elsewhere.

Either way the **anima_lora repo and its running daemon must exist on the same
machine** — the node only submits jobs over localhost HTTP; the daemon (started
with `make daemon` from the repo) is what actually runs the training in
anima_lora's venv. The node does not need to run inside that venv.

## Node

### Anima LoRA Trainer

Inputs:
- `anima_model` — base Anima DiT checkpoint, picked from ComfyUI's
  `diffusion_models` folder (same list as `UNETLoader`). Used as the training
  base **and** reloaded afterwards to build the output MODEL.
- `image` — the IMAGE to train on (single-image mode; each frame in the batch is
  written to a temp dir with the caption).
- `text` — caption for the image.
- `rank` — LoRA rank (`network_dim`); alpha is tied to it.
- `epochs` — number of training epochs.
- `gpu` — hardware tier:
  - `8GB`  → `[low_vram]` preset (gradient checkpointing + unsloth offload)
  - `16GB` → `[default]` preset + `blocks_to_swap=12`
  - `high` → `[32gb]` preset (no swap, no checkpointing)

Output:
- `model` — the chosen base checkpoint loaded and patched with the freshly
  trained LoRA. Use it exactly like the output of the Anima Adapter Loader; no
  separate loader node needed.

The method is locked to `configs/gui-methods/tlora.toml` (T-LoRA).
Saves to
`<ComfyUI>/models/loras/anima_trainer_<timestamp>.safetensors`.

## Notes

- **No MODEL input by design.** Holding the base DiT resident in ComfyUI while
  also spawning a trainer on the same GPU is an easy OOM, so this node does not
  take a MODEL socket. It loads the chosen checkpoint itself *after* training and
  returns it patched.
- **Daemon required.** If the training daemon isn't running, the node errors out
  (it does **not** auto-start it). Start it with `make daemon` (or
  `python tasks.py daemon`) from the `anima_lora/` directory, then re-run.
- **Training is long** (minutes to tens of minutes). The ComfyUI worker blocks
  until the daemon job finishes; a progress bar tracks the run and the UI stays
  responsive. Watch the console for per-step logs.
- **Memory** — the node calls `comfy.model_management.unload_all_models()` before
  training so there is room for the trainer's own DiT + optimizer state.
- **Single-image mode** writes PNGs + `.txt` under
  `<anima_lora_root>/output/tmp_trainer/<timestamp>/`. Prune periodically.
- **Plain LoRA output** — tlora saves as pure LoRA (the rank mask is
  training-only), so the output safetensors is also usable by any ComfyUI LoRA loader.

## Out of scope

- Directory-of-images datasets and method/preset/warm-start overrides (the old
  "Advanced" node) — use the GUI or CLI (`make lora-gui`) for those.
- Baking the trained LoRA into DiT weights as a standalone checkpoint
  (see `scripts/toolkits/merge_to_dit.py` for the CLI equivalent).
