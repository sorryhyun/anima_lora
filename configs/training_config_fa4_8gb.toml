base_config = "base.toml"
dataset_config = "configs/dataset_config.toml"
network_alpha = 32
network_dim = 32
caption_shuffle_variants = 2
checkpointing_epochs = 2
learning_rate = 2e-5
max_train_epochs = 2
save_every_n_epochs = 2
attn_mode = "flash"  # flash4 not supported yet (flash-attention-sm120 disabled)
gradient_checkpointing = true
unsloth_offload_checkpointing = true
cache_llm_adapter_outputs = true
trim_crossattn_kv = false  # trim_crossattn_kv requires flash4 (not supported yet)
blocks_to_swap = 0