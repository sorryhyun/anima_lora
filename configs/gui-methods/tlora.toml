network_dim = 32
network_alpha = 32

learning_rate = 5e-5
max_train_epochs = 4
save_every_n_epochs = 2
checkpointing_epochs = 2

use_shuffled_caption_variants = true
cache_llm_adapter_outputs = true
caption_dropout_rate = 0.1

down_init = "weight_svd"

use_timestep_mask = true
min_rank = 1
alpha_rank_scale = 1.0

output_name = "anima_tlora"

blocks_to_swap = 0

use_cmmd = false

[variant]
family = "tlora"
label = "T-LoRA"
description = "T-LoRA rank mask on SVD-Down LoRA — default mergeable combo."
order = 10
