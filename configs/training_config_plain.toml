base_config = "base.toml"
dataset_config = "configs/dataset_config.toml"
network_alpha = 16
network_dim = 16
caption_shuffle_variants = 2
checkpointing_epochs = 2
learning_rate = 5e-5
max_train_epochs = 2
save_every_n_epochs = 2
attn_mode = "flash4"
cache_llm_adapter_outputs = true
trim_crossattn_kv = true

# layer_start=8
# loss_type = "pseudo_huber"  # smooth L1-like loss, robust to outliers
# pseudo_huber_c = 0.3       # small c -> L1-like, large c -> MSE-like
# multiscale_loss_weight = 0.5  # weight for 2x-downsampled loss term (0 = disabled)