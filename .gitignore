__pycache__/
*.pyc
*.pyo
.venv
*.egg-info/
dist/
build/
output/
*.safetensors
# Ship the SmoothQuant-style per-channel calibration as a vendored asset —
# small (~3.5MB), strictly an input-magnitude prior on the base DiT, not a
# checkpoint. Regenerate via `bench/channel_stats/analyze_lora_input_channels.py`.
!networks/calibration/channel_stats.safetensors
!networks/calibration/cond_channel_stats.safetensors
*.ckpt
*.pt
.DS_Store
*.whl
image_dataset
post_image_dataset/
ip-adapter-dataset/
easycontrol-dataset/
masks/
masks_mit/
masks_sam/
archive/graft/runtime/train_images/
archive/graft/runtime/candidates/
archive/graft/runtime/survivors/
archive/graft/runtime/generation_prompts.txt
/models/
!library/models/
test_output/
output_temp/
/workflows
results
logs
_logs/
.claude
.env
.anima-update-backups
node.zip
_archive/
gui/gui_settings.json
configs/gui-methods/custom/
near_twins_temp.toml
sincos_decensored/
# ResShift SR sidecar — vendored source (sr/resshift/) IS committed; ignore only
# the (now-removed) external clone, the venv, weights, and data artifacts.
/ResShift/
/sr/.venv/
/sr/data/
/sr/**/weights/
/post_image_dataset
# LaTeX build artifacts (paper dirs; main.pdf stays tracked)
project/*/paper/*.log
project/*/paper/*.blg
# E4 exercise eval run with the pre-SFW-amendment renders — keep out of git
project/sigma_lowres/paper_bench/runs/20260729-2137-e4-eval-s1001/
# E4 eval render PNGs (result.json/report.md stay tracked)
project/sigma_lowres/paper_bench/runs/*/renders/
# E14 vector-ledger arm-sum stores (~75 GB fp32) — scalar deliverables stay tracked
project/sigma_lowres/paper_bench/runs/*/arm_sums/
