__pycache__/
.pytest_cache/
*.pyc
*.pyo
.venv
*.egg-info/
dist/
build/
output/
*.safetensors
.vscode/
# Ship the SmoothQuant-style per-channel calibration as a vendored asset —
# small (~3.5MB), strictly an input-magnitude prior on the base DiT, not a
# checkpoint. Regenerate via `bench/channel_stats/analyze_lora_input_channels.py`.
!networks/calibration/channel_stats.safetensors
!networks/calibration/cond_channel_stats.safetensors
*.ckpt
*.pt
.DS_Store
*.whl
image_dataset
post_image_dataset/
ip-adapter-dataset/
easycontrol-dataset/
masks/
masks_mit/
masks_sam/
archive/graft/runtime/train_images/
archive/graft/runtime/candidates/
archive/graft/runtime/survivors/
archive/graft/runtime/generation_prompts.txt
/models/
!library/models/
test_output/
output_temp/
/workflows
results
logs
_logs/
.claude/*
!.claude/skills/
.env
.anima_backend
.anima-update-backups
node.zip
_archive/
gui/gui_settings.json
configs/gui-methods/custom/
near_twins_temp.toml
sincos_decensored/
# ResShift SR sidecar (finished — lives under project/finished/sr/) — vendored
# source (resshift/) IS committed; ignore only the (now-removed) external clone,
# the old venv, weights, and data artifacts.
/ResShift/
/project/finished/sr/.venv/
/project/finished/sr/data/
/project/finished/sr/**/weights/
/post_image_dataset
# LaTeX build artifacts (paper dirs; main.pdf stays tracked)
project/*/paper/*.log
project/*/paper/*.blg
# paper_bench = pure run artifacts (~15 GB, no scripts/docs; nothing ever tracked)
project/sigma_lowres/paper_bench/
bench/cjk_adapter/assets/
bench/cjk_distill/assets/
project/cjk_aware_anima/datasets/assets/

project/sigma_lowres/bench/probe1024_closure
project/sigma_lowres/bench/probe1280_cache

# anime_tools stage reports (report_dir defaults anchor on ANIMA_HOME = this checkout)
workspace/

# cjk lines — rendered contact sheets / montages and judge|OCR machine outputs
# next to the reports (the .md reports stay tracked; u0_map_bands.json was
# tracked before this rule and stays)
project/cjk_aware_anima*/reports/*.png
project/cjk_aware_anima*/reports/*.json
