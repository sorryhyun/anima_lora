"""near_twins — in-artist near-twin variant-pair miner (EasyControl curation tool).

Mines near-duplicate pairs within one artist whose members differ by a specified
attribute (e.g. a speech bubble). Run from the repo root with
``python -m easycontrol_adapters.tools.near_twins``; workflow, pipeline and knobs
are in ``README.md``.
"""
